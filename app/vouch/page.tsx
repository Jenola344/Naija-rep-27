import { VouchInterface } from "@/components/vouch/vouch-interface"

export default function VouchPage() {
  return (
    <main className="min-h-screen bg-background">
      <VouchInterface />
    </main>
  )
}
